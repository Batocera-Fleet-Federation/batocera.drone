name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (for example, v1.2.3)'
        required: true
        type: string
      dry_run:
        description: 'Validate and build without creating a tag or release'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
  push:
    branches:
      - main

permissions:
  contents: write
  packages: write

concurrency:
  group: release-${{ github.repository }}
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      - name: Fetch release tags
        run: git fetch --force --tags origin

      - name: Determine release version
        id: version
        env:
          COMMIT_MESSAGE: ${{ github.event.head_commit.message }}
          REQUESTED_VERSION: ${{ inputs.version }}
          REQUESTED_DRY_RUN: ${{ inputs.dry_run }}
        run: |
          set -euo pipefail

          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            tag="${REQUESTED_VERSION}"
            dry_run="${REQUESTED_DRY_RUN:-false}"
            create_tag=true
            skip=false
          else
            subject="${COMMIT_MESSAGE%%$'\n'*}"
            if [[ "$subject" == "Update changelog for v"* ]]; then
              echo "Skipping the commit created by scripts/create-release.sh."
              echo "skip=true" >> "$GITHUB_OUTPUT"
              exit 0
            fi

            existing_tag="$(
              git tag --points-at HEAD --sort=-v:refname \
                | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' \
                | head -n 1 \
                || true
            )"
            if [[ -n "$existing_tag" ]]; then
              tag="$existing_tag"
              create_tag=false
            else
              latest_tag="$(
                git tag --sort=-v:refname \
                  | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' \
                  | head -n 1 \
                  || true
              )"
              tag="$(scripts/next-release-version.sh "$latest_tag" "$subject")"
              create_tag=true
            fi
            dry_run=false
            skip=false
          fi

          if [[ ! "$tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Version must match vMAJOR.MINOR.PATCH: $tag" >&2
            exit 1
          fi
          if [[ "$create_tag" == "true" ]] && git show-ref --tags --verify --quiet "refs/tags/$tag"; then
            echo "Tag already exists: $tag" >&2
            exit 1
          fi

          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "dry_run=$dry_run" >> "$GITHUB_OUTPUT"
          echo "create_tag=$create_tag" >> "$GITHUB_OUTPUT"
          echo "skip=$skip" >> "$GITHUB_OUTPUT"
          echo "Release version: $tag"

      - name: Build release assets
        if: steps.version.outputs.skip != 'true'
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.tag }}
          UPDATE_SIGNING_PRIVATE_KEY: ${{ secrets.DRONE_UPDATE_SIGNING_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          if [[ -z "$UPDATE_SIGNING_PRIVATE_KEY" ]]; then
            echo "DRONE_UPDATE_SIGNING_PRIVATE_KEY is required to publish verifiable updates." >&2
            exit 1
          fi
          original_version="$(cat app/VERSION 2>/dev/null || true)"
          restore_version() {
            printf '%s\n' "$original_version" > app/VERSION
          }
          trap restore_version EXIT

          printf '%s\n' "$RELEASE_VERSION" > app/VERSION
          mkdir -p dist
          tar \
            --exclude='*/__pycache__' \
            --exclude='*.pyc' \
            -czf dist/drone-app.tar.gz \
            app content

          for asset in \
            scripts/batocera_install.sh \
            scripts/batocera_uninstall.sh \
            scripts/run_now.sh \
            dist/drone-app.tar.gz; do
            test -s "$asset"
          done

          python3 - "$RELEASE_VERSION" <<'PY'
          import hashlib
          import json
          import sys
          from pathlib import Path

          version = sys.argv[1]
          assets = {}
          for path in (
              Path("dist/drone-app.tar.gz"),
              Path("scripts/run_now.sh"),
              Path("scripts/batocera_install.sh"),
              Path("scripts/batocera_uninstall.sh"),
          ):
              payload = path.read_bytes()
              assets[path.name] = {
                  "sha256": hashlib.sha256(payload).hexdigest(),
                  "size": len(payload),
              }
          manifest = {"schema": 1, "version": version, "assets": assets}
          Path("dist/release-manifest.json").write_text(
              json.dumps(manifest, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY

          signing_key="$RUNNER_TEMP/drone-update-signing.pem"
          umask 077
          printf '%s\n' "$UPDATE_SIGNING_PRIVATE_KEY" > "$signing_key"
          openssl dgst -sha256 -sign "$signing_key" \
            -out dist/release-manifest.sig dist/release-manifest.json
          openssl dgst -sha256 -verify app/update-signing-public.pem \
            -signature dist/release-manifest.sig dist/release-manifest.json
          rm -f "$signing_key"

      - name: Set up QEMU
        if: steps.version.outputs.skip != 'true' && steps.version.outputs.dry_run != 'true'
        uses: docker/setup-qemu-action@c7c53464625b32c7a7e944ae62b3e17d2b600130 # v3

      - name: Set up Docker Buildx
        if: steps.version.outputs.skip != 'true' && steps.version.outputs.dry_run != 'true'
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Log in to GHCR
        if: steps.version.outputs.skip != 'true' && steps.version.outputs.dry_run != 'true'
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Drone container image
        if: steps.version.outputs.skip != 'true'
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.tag }}
          RELEASE_DRY_RUN: ${{ steps.version.outputs.dry_run }}
        run: |
          args=(--version "$RELEASE_VERSION")
          if [[ "$RELEASE_DRY_RUN" == "true" ]]; then
            args+=(--dry-run)
          else
            args+=(--push)
          fi
          ./scripts/docker-publish.sh "${args[@]}"

      - name: Create version tag
        if: >-
          steps.version.outputs.skip != 'true' &&
          steps.version.outputs.dry_run != 'true' &&
          steps.version.outputs.create_tag == 'true'
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.tag }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "$RELEASE_VERSION" -m "Batocera Drone $RELEASE_VERSION"
          git push origin "$RELEASE_VERSION"

      - name: Create GitHub release
        if: steps.version.outputs.skip != 'true' && steps.version.outputs.dry_run != 'true'
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Batocera Drone ${{ steps.version.outputs.tag }}
          target_commitish: ${{ github.sha }}
          generate_release_notes: true
          draft: false
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            scripts/batocera_install.sh
            scripts/batocera_uninstall.sh
            scripts/run_now.sh
            dist/drone-app.tar.gz
            dist/release-manifest.json
            dist/release-manifest.sig
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update latest tag
        if: steps.version.outputs.skip != 'true' && steps.version.outputs.dry_run != 'true'
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.tag }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -fa latest -m "Batocera Drone latest release: $RELEASE_VERSION" "${RELEASE_VERSION}^{commit}"
          git push origin refs/tags/latest --force

      - name: Dry-run summary
        if: steps.version.outputs.skip != 'true' && steps.version.outputs.dry_run == 'true'
        run: echo "Validated and built release ${{ steps.version.outputs.tag }} without publishing it."
